# Configurations

This directory intentionally contains no exported configuration files or command transcripts. The lab is documented from the screenshot evidence in the repository.

Exact configuration values, commands, IP addresses, hostnames, and service parameters are **Not confirmed from the available evidence.**
